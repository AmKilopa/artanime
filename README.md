# artanime

Минимальный лендинг для Vercel: большая кнопка запускает обратный отсчет, затем открывается видео без стандартных контролов.

## Команды

```bash
npm install
npm run dev
npm run build
```

Для публикации на Vercel используйте:

```bash
npx vercel --prod
```
